-- Команда продавца: выборка сотрудников по sellerId (список в админке и веер
-- уведомлений о новом заказе). Раньше по этой колонке ходили только точечно.
CREATE INDEX "users_sellerId_idx" ON "users"("sellerId");
